{
  "name": "holydeck",
  "version": "0.0.0",
  "description": "HolyDeck CLI: bible verses for sermons and presentations, from a local revisioned datastore",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "holydeck": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run --coverage",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@holydeck/core": "workspace:*",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "tsup": "^8.5.1",
    "vitest": "^5.0.0"
  }
}
