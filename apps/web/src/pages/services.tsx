// Where a signed-in account lands (`/services`). The actual dashboard lives in `workspace/Dashboard.tsx`
// alongside the rest of the service workspace; this file only keeps `app.tsx`'s import stable.

export { Dashboard as ServicesPage } from '../workspace/Dashboard.js';
