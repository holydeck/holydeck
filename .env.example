# MongoDB root credentials — used once by deploy/mongo-init/01-app-user.js to create the
# application's own least-privilege user. Generate with: openssl rand -hex 32
HOLYDECK_MONGO_ROOT_PASSWORD=change-me

# MongoDB application-user password — app, worker, migrate and the corpus connect with this.
# Generate with: openssl rand -hex 32
HOLYDECK_MONGO_PASSWORD=change-me

# Shared secret between the application and its internal corpus. Generate with: openssl rand -hex 32
HOLYDECK_CORPUS_TOKEN=change-me
